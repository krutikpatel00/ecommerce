
import React from 'react'

const NoPageData = ({ text }) => {
      return (
            <div className='d-flex justify-content-center align-items-center' style={{ height: '100vh' }}>
                  <h4 className='text-black'>{text}</h4>
            </div>

      )
}

export default NoPageData