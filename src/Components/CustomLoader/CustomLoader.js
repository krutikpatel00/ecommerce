import React from 'react';
import './CustomLoader.css'; // Import your custom CSS file

const CustomLoader = () => {
      return (
            <div className="loader-container">
                  <div className="loader"></div>
            </div>
      );
}

export default CustomLoader;
